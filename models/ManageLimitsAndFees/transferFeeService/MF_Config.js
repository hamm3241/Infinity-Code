define([],function(){
	var mappings = {
	};
	Object.freeze(mappings);
	
	var typings = {
	}
	Object.freeze(typings);
	
	var primaryKeys = [
	];
	Object.freeze(primaryKeys);
	
	var config = {
		mappings : mappings,
		typings : typings,
		primaryKeys : primaryKeys,
		serviceName : "ManageLimitsAndFees",
		tableName : "transferFeeService"
	};
	Object.freeze(config);
	
	return config;
})
